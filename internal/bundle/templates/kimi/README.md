# boxer for Kimi Code CLI

Generated by `boxer package kimi` [[.Version]]. Do not edit; regenerate.

## Status

Kimi Code CLI hooks are `[[ "[[hooks]]" ]]` tables in `~/.kimi-code/config.toml` (user level only), speak
the converged protocol on stdin, and can only allow or block (`permissionDecision`, exit 2).
Verified 2026-09-17 against 0.43.1: `updatedInput` is ignored, and Kimi's `Bash` tool does not see
a prepended `PATH`, so shims cannot redirect a bare command either. **Use tool mode with Kimi**:

```toml
[harness.kimi]
mode = "tool"
```

The hook then denies shell use with the exact `boxer run` line, and the `boxer_run` MCP tool is the
way in. Both paths are covered by `boxer-eval` (tier t1) against the real Kimi binary.

## Install

1. Append `hooks.toml` to `~/.kimi-code/config.toml` (or `$KIMI_CODE_HOME/config.toml`).
2. `.kimi-code/mcp.json` → the repository root (`boxer install kimi` does this and merges).
3. `.agents/skills/boxer/SKILL.md` → the repository root (universal skills path).
4. `boxer shim install` and prepend the printed directory to `PATH`.

`boxer` must be on `PATH`; hooks and shims call it by name.
